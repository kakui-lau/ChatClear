# ChatClear 隐私说明（草案）

更新时间：2026-09-11

ChatClear 是本地优先的桌面应用。目前版本不包含分析、广告、云同步或自建后端。

## 本机处理的数据

- Telegram 手机号：仅用于向 Telegram 发起登录
- 用户提供的 Telegram `api_id/api_hash`：使用操作系统安全存储加密后保存在本机
- 可选代理设置：代理地址、端口及认证信息使用操作系统安全存储加密后保存在本机
- 登录验证码与两步验证密码：仅在当前登录步骤的内存中短暂存在
- Telegram 授权会话：由 TDLib 写入应用数据目录
- TDLib 数据库密钥：使用操作系统安全存储加密后写入应用数据目录
- 群组元数据：群组名称、类型、成员数、账号身份、归档状态和最近活动时间
- 白名单：作为本地偏好保存在应用的本地存储中

## 不处理的数据

ChatClear 不主动读取或展示消息正文，不上传用户 session，不出售用户数据，也不向开发者服务器发送遥测。

在 Linux 上，如果系统只能提供 Electron 的 `basic_text` 后备存储，ChatClear 会拒绝保存凭证；
用户需要先启用兼容 Secret Service 的系统密钥环。

## 数据传输

应用通过 TDLib 与 Telegram 基础设施通信。如果用户启用代理，Telegram 网络流量会经过用户指定的代理服务器；该代理运营方可能按其自身规则处理连接元数据。Telegram 对相关数据的处理由 Telegram 自身条款和隐私政策约束。

## 注销

点击“注销”会调用 Telegram 的注销接口，使当前 TDLib 授权会话失效。操作系统仍可能保留不含有效授权的应用缓存；用户可以通过卸载应用并删除其应用数据目录彻底清理本地数据。

本文件是发行前草案，正式发布时应补充发行主体、联系渠道、适用地区和用户权利说明。
