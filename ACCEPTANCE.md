# ChatClear 0.2.0 验收记录

验收日期：2026-09-11

## 结果

开发版核心流程和 macOS arm64 安装包通过本地验收，可以继续进行真实 Telegram 测试。当前 macOS 包尚未使用 Developer ID 签名和公证，不应直接作为正式发行版发布。

## 已通过

- `pnpm lint`
- `pnpm format:check`
- `pnpm typecheck`
- `pnpm test`：22 项连接设置校验测试通过
- `pnpm build`
- `pnpm audit --prod`：未发现已知漏洞
- 首次启动显示用户凭证配置页
- API ID 与 API Hash 格式校验
- 凭证保存后进入手机号登录页
- 清除连接设置后回到首次配置页
- SOCKS5 与 HTTP CONNECT 代理字段可用
- 登录与配置窗口按内容使用紧凑尺寸，群组管理页自动切换为大窗口
- 无效手机号在本地拦截，不发送网络请求
- 群组名称、类型和位置筛选
- 本地保护名单切换
- 群主会话强制锁定
- 管理员会话退出警告
- 确认文字不匹配时禁止退出
- 串行退出进度与完成后列表刷新
- 跳过链接、键盘焦点与原生确认对话框 Escape 取消
- 页面运行期间无控制台错误
- macOS arm64 应用进程可从隔离用户数据目录启动
- ChatClear 品牌图标已应用到界面、应用包和 DMG/ZIP 构建配置
- DMG 校验和有效
- arm64 TDLib 动态库已包含在安装包中

## 凭证与生产包检查

- 安装包中不存在 `.env` 或 `.env.local`
- 安装包中不存在构建时 Telegram 凭证注入代码
- 用户在开发期间提供的 API ID/API Hash 未出现在 0.2.0 构建产物中
- 仅开发模式可用的验收数据未出现在生产渲染包中
- 运行时连接设置保存到操作系统安全存储加密文件，渲染进程不会读回明文

## 尚未执行

- 真实验证码登录
- 真实群组同步
- 真实退群
- 断网恢复后的真实 Telegram 登录

原因：验收机器当前无法直连 Telegram，8 秒网络探测超时，并且未发现可用的本地 HTTP/SOCKS 代理。以上操作还需要测试账号、有效代理及一次性验证码；真实退群应只对专用测试群执行。

## 正式发布前阻塞项

- 配置 macOS Developer ID 签名与公证
- 在测试账号和专用测试群上完成真实链路验收
- 配置 Windows 构建与 Authenticode 签名（如需发布 Windows 版）
