# 更新记录

本项目遵循语义化版本号。

## 0.3.2 - 2026-09-11

- 将 API 凭证、代理认证信息和 TDLib 数据库密钥改为 AES-256-GCM 本地文件加密，不再调用系统钥匙串，避免 macOS 钥匙串授权窗口。
- 显式关闭 Electron Cookie 系统加密 fuse，防止底层 Cookie 存储间接访问钥匙串。
- 本地随机主密钥使用仅限当前用户的文件权限保存；补充密文篡改、用途隔离、权限修复和凭证持久化测试。
- 由于旧版钥匙串密文无法在无授权提示的前提下迁移，从 0.3.1 或更早版本升级后需要重新配置并登录一次；旧 session 不会被新版本加载。
- 重构桌面端顶部栏：统一品牌、快捷操作和账户信息层级，补充图标、交互反馈、深色主题与窄窗口自适应。
- 将弹窗和任务进度卡片的“关闭”入口改为带图标的实体按钮，扩大点击热区并增强悬停、按下与键盘焦点反馈。

## 0.3.1 - 2026-09-11

- 修复未配置 Apple Developer ID 时 macOS 应用包签名不完整，导致从浏览器下载后被 Gatekeeper 判定为“已损坏”的问题。
- 无证书构建现在会对完整应用进行临时签名并使用兼容原生 TDLib 的受限权限文件；配置 Developer ID 后仍保持正式签名与公证流程。

## 0.3.0 - 2026-09-11

- 将主窗口改为单一表格滚动区域，减少长列表滚动时的重排与嵌套滚动冲突。
- 新增 Telegram 联系入口与赞助地址复制面板。
- 新增组合智能筛选、筛选方案、管理员默认保护、7 天观察清单和 90 天未活跃快捷选择。
- 新增归档、取消归档、静音、清除本人聊天历史等批量操作。
- 新增 Telegram 限流自动等待、任务安全暂停、断点恢复、失败重试和本地审计历史。
- 新增 CSV 报告导出、本地偏好备份与恢复；备份不包含 API 凭证或 Telegram session。
- 新增最多 10 个本地账号，账号之间使用独立 TDLib 数据目录和加密密钥。
- 新增简体中文/英文、浅色/深色/跟随系统主题和启动更新检查。
- 新增默认关闭的崩溃报告开关；未配置上报地址时不会上传。
- 新增 macOS Hardened Runtime 权限文件和跨平台 GitHub Release 构建流程。
- 补强账号切换竞态防护、异步错误反馈和批量任务持久化测试。

## 0.2.3 - 2026-09-11

- 为 macOS 隐藏式标题栏增加窗口控制按钮安全区，避免红黄绿按钮遮挡 Logo。

## 0.2.2 - 2026-09-11

- 修复打包后应用内 Logo 使用绝对路径导致的破图。
- 将旧版 Base64URL 数据库密钥兼容转换为 TDLib 要求的标准 Base64。
- 登录前快速检测代理端口，并为代理未运行或端口错误提供明确提示。
- 避免同一登录错误同时显示为状态信息和错误信息。
- 代理端口不再预填，避免把示例端口误认为当前代理软件的实际配置。

## 0.2.1 - 2026-09-11

- 保存连接设置后立即进入登录页，不再等待 TDLib 或代理完成初始化。
- 将登录超时覆盖到客户端与代理初始化阶段，避免无期限停留在连接状态。
- 应用启动恢复会话超过三秒时先显示登录界面，恢复成功后再自动进入群组管理页。

## 0.2.0 - 2026-09-11

- 完成用户自配置 Telegram API 凭证与本机加密存储。
- 支持 SOCKS5、HTTP CONNECT 代理和连接超时恢复。
- 支持群组同步、筛选、白名单、群主锁定、管理员警告和串行退出。
- 新增 ChatClear（群清）品牌 Logo 及 macOS、Windows、Linux 打包图标。
- 新增单实例、防外部导航、权限拒绝、渲染错误恢复和 Linux 安全存储检查。
- 新增连接设置单元测试、GitHub Actions、贡献指南、安全策略和验收记录。
